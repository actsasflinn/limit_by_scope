sqlite:
  :adapter: sqlite
  :dbfile: limit_by_scope_plugin.sqlite.db
sqlite3:
  :adapter: sqlite3
  :dbfile: limit_by_scope_plugin.sqlite3.db
postgresql:
  :adapter: postgresql
  :username: postgres
  :password: postgres
  :database: limit_by_scope_plugin_test
  :min_messages: ERROR
mysql:
  :adapter: mysql
  :host: localhost
  :username: root
  :password:
  :database: limit_by_scope_plugin_test